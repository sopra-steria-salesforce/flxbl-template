#!/bin/bash
# This script installs pre dependencies after all package deployments for every scratch org in the pool
# (referenced in postDeploymentScriptPath attribute in project-[validate/dev]-pool-def.json)
